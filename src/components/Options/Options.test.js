import "React";
import Options from "./Options";

import { render, fireEvent } from "@testing-library/react";

describe("Options", () => {
  const mockData = {};

  it("should be able to edit form fields and submit");
  it("should not revert form fields to default on submit");
});
